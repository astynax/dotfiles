# -*- mode:shell-script -*-

if [[ -z "$1" ]]; then
    echo "Usage: mcd NAME"
elif [[ -e "$1" ]]; then
    echo "The disrectory $1 alredy exists, cd'ing into..."
    cd "$1"
else
    mkdir -p "$1" && cd "$1"
fi
